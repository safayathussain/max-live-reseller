import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
      lightGray: {
        main: '#A7A7A7', // Your primary color
      },
      secondary: {
        main: '#ffc107', // Your secondary color
      },
    },
  });